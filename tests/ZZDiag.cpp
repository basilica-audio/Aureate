// Intentionally empty. This was a throwaway diagnostic used while calibrating
// IronStage's ADAA conditioning. It is untracked and never committed; it is
// left as an empty translation unit rather than deleted because this session
// is not permitted to remove files. Safe to delete.
